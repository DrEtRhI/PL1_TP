#include <stdio.h>#define TAILLE 100typedef char prenom[20];typedef struct {prenom nom; int age;} Personne;void creationPersonne(Personne* p, int x, prenom nom);int main(){		Personne p1;	creationPersonne(&p1, 12, "Paul");	printf("identite personne\nprenom : %s\nage : %d\n", p1.nom, p1.age); return 0;}void creationPersonne(Personne* p, int x, prenom nom){	(*p).age = x;	(*p).nom = nom;}